# iot-hub-device-twin-match
Checks if there are differences in device twin reported/desired properties while ignoring metadata.

# Usage
Build the project and run 
```
dotnet .\DeviceTwinMismatch <iot hub connection string>
```